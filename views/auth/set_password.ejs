<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      .form-group {
        margin-bottom: 15px;
      }

      label {
        display: block;
        font-weight: bold;
        margin-bottom: 5px;
      }

      input {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
      }

      button {
        background-color: #007BFF;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }
      /* #error-message {
        color: red;
        font-size: 14px;
        margin-top: 5px;
      } */
    </style>
    <style>
        .notification {
          position: fixed;
          top: 10px;
          left: 50%;
          transform: translateX(-50%);
          background-color: #f2dede;
          color: #a94442;
          border: 1px solid #ebccd1;
          border-radius: 4px;
          padding: 15px;
          z-index: 999;
          display: none;
        }
      </style>
    <script>
        // Hide error message after 9 seconds
        window.onload = function() {
          setTimeout(function() {
            var errorMessage = document.getElementById("errorMessage");
            errorMessage.style.display = "none";
          }, 9000);
        };
      </script>
    
  </head>

  <body>
    <div class="container">
      <h2><%=DATA.perpose%></h2>
      <% if (!DATA.valid) { %>
      <!-- <div id="errorMessage" class="error-message"> -->
        <div class="notification" id="errorMessage" style="display: block;">
        <% DATA.errors.forEach(error => { %>
        <p><%= error %></p>
        <% }); %>
      </div>
      <% } %>
      
      <form id="passwordSetForm" action="<%= DATA.backend_url %>" method="POST">
        <input type="hidden" name="_token" value="<%= DATA.token %>">
        <div class="form-group">
          <label for="password">Password:</label>
          <input type="password" id="password" name="password" required>
        </div>
        <div class="form-group">
          <label for="confirmPassword">Confirm Password:</label>
          <input type="password" id="confirmPassword" name="confirmPassword" required>
        </div>
        <!-- <button type="button" onclick="submitForm()">Set Password</button> -->
        <button type="submit">Set Password</button>
        <!-- <span id="errorMessage" class="error-message" style="color: red"></span> -->
      </form>
    </div>
  </body>
  </html>